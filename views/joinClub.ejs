<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join The Club</title>
</head>
<body>
    <%-include('partials/navbar') %>
    <form action="/join-club" method="POST">
        <label for="vip-password">What's The Secret Password</label>
        <input type="password" name="vip-password" id="vip-password">
        <button>Enter The Club</button>
    </form>
</body>
</html>